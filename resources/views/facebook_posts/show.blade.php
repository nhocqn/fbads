@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">facebook_post {{ $facebook_post->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('/facebook_posts') }}" title="Back"><button class="btn btn-warning btn-xs"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/facebook_posts/' . $facebook_post->id . '/edit') }}" title="Edit facebook_post"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('facebook_posts' . '/' . $facebook_post->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-xs" title="Delete facebook_post" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $facebook_post->id }}</td>
                                    </tr>
                                    <tr><th> Facebook Post Id </th><td> {{ $facebook_post->facebook_post_id }} </td></tr><tr><th> Created At </th><td> {{ $facebook_post->created_at }} </td></tr><tr><th> Updated At </th><td> {{ $facebook_post->updated_at }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
